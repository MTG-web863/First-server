class Archer {

    constructor(name, gender, profession, age, strength, hitpoints, weapons) {
        this.name = name;
        this.gender = gender;
        this.profession = profession;
        this.age = age;
        this.strength = strength;
        this.hitpoints = hitpoints;
    
    }
    

    printStats (){
        console.log("************" + name: " + this.name + "\n" + "strength: " + this.strength
            "hitpoints: " + this.strength "age" + this.age);
    };
    
    isAlive()
        if (hitpoints > 0 ){
            console,log(this.name + "is alive!")
        }
    };

    attack(enemy){
        enemy.hitpoints = enemy.hitpoints - Link.strength
    }
};
const Link = new Archer("Link", "Male", "Shooter" 20, 30, 100)
const Bob 

bob.fightingStyle = "Scratch!"
Link.power = "Sword Slash!"

Bob.printStats();
Link.attack(Bob);
Bob.printStats();
Link.levelUp();
Link.printStats()
Bob.levelUp()

function battle(char1, char2) {
    char1,attack(char2)
    char2,attack(char1)
    char1,printStats
    if (char1.hitpoints > char2.hitpoints) {
        console.log(char1)
        
    }
}


    action(){
        if(this.attack === true) {
            console.log(this.shoot);
            
        }
    }
}

let RobinHood = new Archer
let BadArcher = class Archer

console.log(Archer)

const RobinHood = {
    age : 500,
    species : Human,
    action (shoot) {
        if (attack === true)
    }

}